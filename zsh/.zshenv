# Set Neovim as the preferred editor for local and remote sessions. If either
# the VISUAL or EDITOR environment variable contains the string 'vi' when the
# shell starts, then the shell will use vi emulation - insert mode.
export EDITOR=nvim
export VISUAL=nvim
